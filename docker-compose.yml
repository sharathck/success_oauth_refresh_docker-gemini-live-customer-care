version: '3.8'

services:
  websocket-demo-app:
    build: .
    ports:
      - "8000:8000"  # Frontend (nginx)
    environment:
      - DEBUG=false
    volumes:
      # Optional: mount logs for debugging
      - ./logs:/var/log/supervisor
    restart: unless-stopped
    container_name: websocket-demo-app